# Monitoring Setup

**Description**
Setup Prometheus and Grafana to track API load, scraping success rates, and DAG failures.

**Acceptance Criteria:**
- [ ] `prometheus.yml` configured
- [ ] `grafana_dashboard.json` imported